import React from 'react';
import logo from '../logo.svg';

const Logo = () => (
  <img 
    src={logo} 
    alt="UR Logo" 
    style={{
      height: '40px',
      width: 'auto',
      marginRight: '10px'
    }}
  />
);

export default Logo;

